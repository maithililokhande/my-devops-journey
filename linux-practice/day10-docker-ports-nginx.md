# DAY 10 - Docker Ports and Web Server
 
- Docker containers can expose services using ports
- `-p host_port:container_port` maps ports
- Nginx runs on port 80 inside container
- Accessed container using browser via localhost
- `docker exec` is used to enter running container
- Containers can be stopped and removed safely
